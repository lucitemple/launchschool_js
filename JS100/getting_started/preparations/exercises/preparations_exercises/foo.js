// 3. Create a file named foo.js in a directory named preparations_exercises. Add the following code:

let foo = "bar";
console.log(foo);
foo;

// 4.1 Use node to run the foo.js file using node. What does it output?
// => bar

// 4.2 Copy and paste the code from foo.js into the node REPL. What does it output?
// let foo = "bar"; => undefined (return value)
// console.log(foo); => bar (output)
// => undefined (return value)
// foo; => 'bar' (return value)

// 4.3 Copy and paste the code from foo.js into the Chrome console REPL. What does it output?
// => bar (output)
// => "bar" (return value)

/* let foo = ["a", "b", "c"];
console.log(foo.length); // => 3
console.log(foo[3]); */
